pub struct KeyboardBuild {
    
}

impl KeyboardBuild {
    fn build() {

    }

    // Target, Vec<Layers>

    
}

// May need to read the files into an expected yaml format first
// targets first
// targets become external categories
// then read the layers as classified by targets
// 
